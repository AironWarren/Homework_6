const int MAX_SIZE = 100;
const int ROW_MASS = 3;
const int COL_MASS = 3;

namespace mass
{
	int cntNegativeElement(float mass[MAX_SIZE], int size);
	float maxElement(float mass[MAX_SIZE], int size);
	float sumElementBelowDiagonal(float mass[ROW_MASS][COL_MASS], int, int);
}

float s—ircle(float R);
void roots(float a, float b, float c);
